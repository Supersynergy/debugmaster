# Social Preview Brief

Target: GitHub repository social preview.

Size: `1280x640`.

Format: PNG or JPG, under 1 MB.

Concept:

- Dark high-contrast technical dashboard.
- Left side: large wordmark `Debugmaster`.
- Subtitle: `Fail-safe repo diagnostics for humans and AI agents`.
- Right side: simplified terminal/report panels showing `PASS`, `WARN`, `FAIL`, `AI brief`, `batch`, and `autofix`.
- Avoid tiny text, busy gradients, real screenshots with private paths, or unrelated stock imagery.

Suggested prompt:

```text
Create a clean 1280x640 GitHub social preview image for "Debugmaster", a fail-safe repository diagnostics CLI. Use a crisp technical dashboard style with dark neutral background, high-contrast white text, subtle blue and green accents, a terminal panel, JSON report card, and small PASS/WARN/FAIL status chips. Main headline: Debugmaster. Subtitle: Fail-safe repo diagnostics for humans and AI agents. Keep text centered in safe area, no logos, no private paths.
```

Provenance:

- This repo currently ships the brief, not a generated image.
- If a generated image is added later, record generator, date, prompt, and license assumptions here.
